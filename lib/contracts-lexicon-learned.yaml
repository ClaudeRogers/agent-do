# Agent-derived contract classifications — REGENERABLE, do not hand-edit.
#
# Produced by the contracts-exception-classifier workflow (2026-06-11):
# parallel agents read each tool's --help and source for verbs the hand
# lexicon (contracts-lexicon.yaml) could not classify, and returned a beat
# assignment with confidence and evidence. Only high/medium confidence
# entries land here; low-confidence verbs stay in the exceptions report.
#
# Merge order: these load FIRST; hand-written overrides in
# contracts-lexicon.yaml always win. To correct an entry, add the verb to
# the hand lexicon's overrides — do not edit this file.
overrides:
  3d:
    view:
      beats:
      - interact
      confidence: medium
      reason: "cmd_view launches a GUI viewer (qlmanage -p / open / meshlab& / open3d draw_geometries)\
        \ \u2014 a side-effecting open, not a structured read. Beat varies a little by installed viewer\
        \ but always an interactive launch."
  agent:
    accept:
      beats:
      - interact
      confidence: high
      reason: "--help: 'Accept pending changes (press y)' \u2014 sends a keystroke to the agent TUI to\
        \ confirm/apply changes. Mutates agent-side state. Not destructive."
    reject:
      beats:
      - interact
      confidence: high
      reason: "--help: 'Reject pending changes (press n)' \u2014 sends keystroke to decline pending changes.\
        \ Mutates agent-side state; declining is not data loss, so not destructive."
    spawn:
      beats:
      - connect
      confidence: high
      reason: "cmd_spawn creates a new tmux session running an agent (claude/opencode/droid/amp) \u2014\
        \ brings the tool into a usable session state. Command returns after launch (or after --wait readiness);\
        \ the long-lived process is the spawned agent, not this call."
  appleevents:
    cache:
      attributes:
      - polymorphic
      - destructive
      beats:
      - snapshot
      - interact
      confidence: high
      reason: 'Source: ''cache status'' globs CACHE_DIR (read-only -> snapshot); ''cache clear'' path.unlink()s
        cached dictionaries (delete -> interact + destructive). Subcommand selects the beat at call time.'
    dictionary:
      beats:
      - snapshot
      confidence: high
      reason: '--help: ''Show scripting dictionary: json, markdown, raw''. Read-only dump of an app''s
        sdef (caching it is a benign read-cache side effect, no world mutation).'
    probe:
      beats:
      - snapshot
      confidence: high
      reason: '--help: ''Resolve app, inspect static scriptability, and load dictionary metadata''. Read-only
        inspection of static bundle scriptability; listed under READ-ONLY (distinct from the LIVE-GATED
        permissions/run verbs).'
    terms:
      beats:
      - snapshot
      confidence: high
      reason: '--help: ''Search commands, classes, and properties from the dictionary''. Read-only query
        over parsed dictionary metadata.'
  auth:
    import-browser:
      attributes:
      - composite
      - sensitive
      beats:
      - connect
      - save
      - verify
      confidence: medium
      reason: 'cmd_import_browser: browser_import creates a usable browse session (connect), apply_validation
        checks the state (verify), then persist_browse_alias/write_session_meta persists the imported
        auth for reuse (save). Persists secret auth material -> sensitive; multi-beat orchestration ->
        composite.'
    instructions:
      beats:
      - snapshot
      confidence: high
      reason: cmd_instructions reads the profile/session-meta and prints recommended next-step guidance.
        Pure read, no mutation or persistence.
    load:
      beats:
      - connect
      confidence: high
      reason: "cmd_load calls load_saved_session to restore previously saved authenticated browser state\
        \ into a usable session \u2014 bring-into-usable-state attach. Consumes stored secrets but does\
        \ not emit or newly persist them, so not sensitive under the strict definition."
  bluetooth:
    scan:
      beats:
      - snapshot
      confidence: high
      reason: 'cmd_scan runs blueutil --inquiry 10 (macOS) or bluetoothctl scan/devices (Linux): a bounded
        ~10s discovery that returns the device list. Read-only enumeration; bounded, so not long_running.'
  browse:
    auth check-creds:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Check if credentials exist for domain'. Reports existence only (does not output\
        \ the secret), so read-only snapshot, not sensitive \u2014 contrast get-creds which would emit\
        \ them."
    auth store-creds:
      attributes:
      - sensitive
      beats:
      - save
      confidence: high
      reason: '--help: ''Store credentials for future sessions'' (DOMAIN_EMAIL/DOMAIN_PASSWORD). Persists
        secret material for reuse -> save + sensitive.'
    clipboard paste:
      beats:
      - interact
      confidence: high
      reason: "--help: 'Send paste shortcut to the page'. Dispatches a paste keystroke into the page \u2014\
        \ mutates page/input state."
    session load:
      beats:
      - connect
      confidence: high
      reason: "Sends session_load action: creates a new browser context seeded with saved cookies+localStorage\
        \ and navigates to the saved URL \u2014 brings the browser into a usable authenticated state.\
        \ Consumes saved auth but does not emit/persist new secrets, so not sensitive under strict definition."
    vision analyze:
      beats:
      - snapshot
      confidence: medium
      reason: '--help: ''Analyze for patterns (errors|forms|login|products|navigation)''. AI-powered read
        of the current page screenshot returning a structured assessment; no world mutation. Medium because
        the JS side-effect surface wasn''t read line-by-line.'
    vision compare:
      beats:
      - snapshot
      confidence: medium
      reason: "--help: 'Compare screenshots for changes'. Read-only diff reporting changes, not a purposive\
        \ pass/fail/block \u2014 per rules a verification-usable read is snapshot, not verify."
    vision explain:
      beats:
      - snapshot
      confidence: medium
      reason: "--help: 'Predict what will happen if action taken'. Predicts an outcome WITHOUT performing\
        \ the action \u2014 read-only inference over current state."
  burp:
    issues:
      beats:
      - snapshot
      confidence: high
      reason: cmd_issues GETs /burp/scanner/issues and prints (optionally severity-filtered). Read-only
        listing of found findings.
    proxy:
      beats:
      - interact
      confidence: high
      reason: cmd_proxy on/off runs networksetup -setwebproxy/-setsecurewebproxy to mutate system network
        proxy settings. Both states mutate, so not polymorphic in beat; toggling off is not data loss.
    scan:
      beats:
      - interact
      confidence: high
      reason: "cmd_scan PUTs the URL into scope then POSTs spider + active scanner \u2014 an async deploy-style\
        \ trigger that returns immediately ('Scan initiated'). Mutating trigger -> interact; returns,\
        \ so not long_running."
  cad:
    measure:
      beats:
      - snapshot
      confidence: high
      reason: cmd_measure runs FreeCAD headless to read shape BoundBox/Volume/Area/CenterOfMass and print
        them. Read-only extraction of dimensions from the file.
    view:
      beats:
      - interact
      confidence: medium
      reason: "cmd_view launches a CAD GUI viewer (librecad/freecad& or macOS open) by file extension\
        \ \u2014 side-effecting open. Medium since exact behavior depends on installed viewer, but all\
        \ paths are an interactive launch."
  ci:
    trigger:
      beats:
      - interact
      confidence: high
      reason: '--help: ''trigger <workflow>'' dispatches a CI run via gh; async deploy-style mutation,
        returns immediately.'
  clerk:
    enterprise-connections:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'List enterprise connections (SAML/OIDC)' \u2014 read-only GET list."
    jwt-templates:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'List JWT templates' \u2014 read-only GET list; templates not secret material."
    oauth-apps:
      beats:
      - snapshot
      confidence: high
      reason: "--help 'List OAuth applications'; source cmd_oauth_apps (l.952-967) prints only id/name/client_id\
        \ + [public] flag, never client_secret \u2014 read-only, not sensitive."
    org-add-member:
      beats:
      - interact
      confidence: high
      reason: "--help: 'org-add-member <org> <user> [--role]' \u2014 POST adds member to org, state mutation."
    org-members:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'List members' \u2014 read-only GET list."
    roles:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'List organization roles' \u2014 read-only GET list."
  clipboard:
    paste:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Print clipboard contents' \u2014 read-only dump of clipboard state, no side effects.\
        \ (Output is arbitrary and could echo secrets a user copied, but the verb is not secret-handling\
        \ by design.)"
  cloudflare:
    analytics:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Raw GraphQL query against Cloudflare Analytics' \u2014 Analytics GraphQL endpoint\
        \ is read-only; arbitrary query string but cannot mutate, so snapshot not passthrough."
    bandwidth:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Bandwidth summary' \u2014 read-only analytics aggregate."
    dns:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'dns <zone>  List DNS records' \u2014 read-only list (add/del/update are separate\
        \ verbs)."
    firewall-events:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Recent firewall events' \u2014 read-only security event log dump."
    r2-buckets:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'List R2 buckets' \u2014 read-only list."
    requests:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Request summary: total, cached, threats blocked' \u2014 read-only analytics aggregate."
    threats:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Security threats: blocked, types, top source countries' \u2014 read-only analytics\
        \ aggregate."
    top-countries:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Top countries by visitor count' \u2014 read-only analytics aggregate."
    top-pages:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Top pages by request count' \u2014 read-only analytics aggregate."
    visitors 24h:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'visitors <zone> [--since N]' default 24h \u2014 read-only visitor analytics summary."
    visitors 7d:
      beats:
      - snapshot
      confidence: high
      reason: "--help: same 'visitors' verb with --since 7d \u2014 read-only visitor analytics summary,\
        \ window is only a flag."
  colab:
    run:
      beats:
      - interact
      confidence: high
      reason: "source cmd_run: 'jupyter nbconvert --execute --to notebook --inplace' executes notebook\
        \ cells (arbitrary code, real side effects) and rewrites the file in place \u2014 a transform/mutation.\
        \ Not flagged passthrough because it is a purpose-built notebook executor, not a generic eval\
        \ string; the inplace file mutation is the dominant contract."
  context:
    build:
      beats:
      - interact
      confidence: high
      reason: "source lib/build.sh cmd_build: walks a content dir for DOC.md/SKILL.md, parses frontmatter,\
        \ packages into an output dir (default dist) \u2014 file transform A->B producing artifacts."
    cache pin:
      beats:
      - interact
      confidence: high
      reason: "source lib/cache.sh _cache_pin: appends pkg id to _pins.json so it is never evicted \u2014\
        \ persistent state-mutation policy flag, not a new saved result."
    crawl:
      attributes:
      - composite
      beats:
      - interact
      - save
      confidence: high
      reason: "source lib/html.sh cmd_crawl: fetches bounded same-origin HTML, extracts text/metadata,\
        \ and indexes content into the persistent ~/.agent-do/context library \u2014 one call does network\
        \ fetch+transform (interact) plus durable persistence for future sessions (save)."
    stale:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Show stale or failed packages' \u2014 read-only status report; merely-usable read,\
        \ not a purposive pass/fail, so snapshot not verify."
  coord:
    alias:
      beats:
      - interact
      confidence: high
      reason: "--help/source alias_self: 'Assign a friendly alias to this agent' \u2014 writes alias into\
        \ the agent presence record, state mutation."
    claim soft:
      beats:
      - interact
      confidence: high
      reason: "source: 'claim <path> --strength soft' (default) writes an advisory claim into claims.json\
        \ \u2014 coordination state mutation, not destructive."
    claim strong:
      beats:
      - interact
      confidence: high
      reason: "source: 'claim <path> --strength strong' writes a stronger advisory lock into claims.json\
        \ \u2014 same mutation path, higher strength field; not destructive (release is separate)."
  creds:
    platform:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'Show detected credential backend platform' \u2014 read-only, reports backend name\
        \ only, no secret values."
    required:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'required <tool>  List secrets declared for a tool' \u2014 read-only list of secret\
        \ KEY NAMES only, no values emitted, so not sensitive."
  debug:
    backtrace:
      beats:
      - snapshot
      confidence: medium
      reason: Reads the current call stack of an attached debug session (standard debugger bt); pure read.
        Verb not in the bash dispatch (--help lists pdb cmds without backtrace), so wiring is ambiguous
        but semantics are unmistakable.
    break:
      beats:
      - interact
      confidence: high
      reason: "--help: 'break <file:line> Set breakpoint' \u2014 mutates debugger session state."
    continue:
      beats:
      - interact
      confidence: high
      reason: "--help: 'continue Continue execution' \u2014 advances/resumes program execution, mutating\
        \ run state."
    print:
      beats:
      - snapshot
      confidence: medium
      reason: "--help: 'print <expr> Print expression' \u2014 reads/displays a value from the frame. Medium:\
        \ pdb print evaluates an arbitrary expression, which could in principle have side effects, but\
        \ documented purpose is read."
    step:
      beats:
      - interact
      confidence: high
      reason: "--help: 'step Step into' \u2014 advances execution one statement, mutating run state."
  dns:
    lookup:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'lookup <domain> [type] DNS lookup' \u2014 read-only resolver query returning records,\
        \ no side effects. Not verify: it dumps records rather than asserting a condition (that would\
        \ be a dns-check)."
  docker:
    compose:
      attributes:
      - polymorphic
      beats:
      - interact
      - snapshot
      confidence: medium
      reason: 'cmd_compose dispatches by subcommand: ''up'' runs ''docker compose up -d'' (interact, detached
        so returns), ''down'' is graceful teardown (interact, not destructive), ''logs''/''ps'' read (snapshot),
        plus a generic passthrough to any compose subcommand. Beat is payload-decided.'
    start:
      beats:
      - interact
      confidence: high
      reason: "--help: 'start <name> Start container' \u2014 brings a stopped container to running; mutates\
        \ world state. Not destructive."
  dpt:
    baseline:
      attributes:
      - composite
      beats:
      - snapshot
      - save
      confidence: high
      reason: cmd_baseline injects the engine to scan the live page (snapshot) then 'cp $DPT_RESULT_FILE
        /tmp/dpt-baseline.json' (save) so a later diff can compare. One call does both beats.
    build:
      beats:
      - interact
      confidence: high
      reason: "cmd_build runs bin/build to rebuild the scoring engine from source \u2014 an A->B compile/transform\
        \ producing dist artifacts. Idempotent rebuild, not destructive."
    report:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'report Narrative design critique report' \u2014 runs the engine read-only over\
        \ the live page via page.evaluate and emits a critique; no side effects."
    scan:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'scan Full scan -> structured JSON' \u2014 read-only structured dump of the page's\
        \ design metrics."
    score:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'score score + grade + dimensions' \u2014 read-only quality read of the current\
        \ page. The score is a USABLE metric, not a purposive pass/fail gate, so snapshot not verify."
    violations:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'violations Violations sorted by impact' \u2014 read-only extraction of failing\
        \ checks from the scan; no side effects."
  email:
    code:
      attributes:
      - composite
      - sensitive
      beats:
      - verify
      - snapshot
      confidence: high
      reason: handle_code always calls wait_for_message (polls every interval until a matching message
        arrives or times out, default --timeout 120) = block-until-condition (verify), then extract_code
        reads the value (snapshot). Emits a verification code = secret material (sensitive).
    latest:
      beats:
      - snapshot
      confidence: high
      reason: "handle_latest calls find_message once (no polling loop) and emits the latest matching message\
        \ \u2014 read-only dump of mailbox state."
    link:
      attributes:
      - composite
      - sensitive
      beats:
      - verify
      - snapshot
      confidence: high
      reason: handle_link always calls wait_for_message (block-until matching message or timeout, default
        120) = verify, then extract_link reads the URL = snapshot. Emits a magic login link = secret auth
        material (sensitive).
  eval:
    compare:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'compare <a> <b> Compare two runs' \u2014 reads two saved result JSON files and\
        \ diffs them; read-only, no side effects."
    results:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_results lists or shows saved *.json result files under EVAL_DIR \u2014 read-only dump\
        \ of prior run state."
    run:
      attributes:
      - composite
      beats:
      - interact
      - verify
      - save
      confidence: high
      reason: 'Source: for each case calls the Anthropic API (and type=code executes code) = interact/side
        effects; evaluates pass/fail per case (exact/contains/regex/rubric/code) = purposive verify; then
        writes a timestamped results JSON (lines 132-135) = save. One call performs all three.'
  excel:
    formula:
      beats:
      - interact
      confidence: high
      reason: "--help: 'formula <cell> <expr> Set formula' \u2014 writes a formula into a cell, mutating\
        \ the workbook."
    sheet name:
      beats:
      - interact
      confidence: medium
      reason: '''sheet <name>'' switches the active sheet, mutating agent-side session context that subsequent
        reads/writes target. Borderline connect-vs-interact: it is a non-destructive selection within
        an already-open workbook (open is the real connect), so classified as agent-side state mutation
        = interact.'
    sheets:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'sheets List all sheets' \u2014 read-only enumeration of workbook structure."
  gcp:
    apis:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'apis <project> List enabled APIs' \u2014 read-only listing via REST API; no mutation,\
        \ no secret material."
    service-accounts:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'service-accounts <project> List service accounts' \u2014 read-only listing of\
        \ SA identities (not keys), no side effects. Key creation/listing are separate sa-key-* verbs."
  gh:
    approve:
      beats:
      - interact
      confidence: high
      reason: "cmd_review_action with --approve runs `gh pr review --approve` \u2014 posts an approval\
        \ review, mutating PR review state (source tools/agent-gh:1411,1656)."
    checkout:
      beats:
      - interact
      confidence: medium
      reason: "cmd_checkout runs `gh pr checkout` \u2014 creates/switches a local branch, a working-tree\
        \ mutation; connect-flavored prep but a real local state change, so interact (source :1507)."
    comment:
      beats:
      - interact
      confidence: high
      reason: "cmd_review_action with --comment runs `gh pr review --comment` \u2014 posts a PR comment,\
        \ mutates the PR thread (source :1411,1658)."
    doctrine:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_doctrine prints the static REVIEW_DOCTRINE constant \u2014 read-only emit, touches\
        \ no state (source :1372). Mirrors latex `template` snapshot precedent."
    draft:
      beats:
      - interact
      confidence: high
      reason: "cmd_ready(draft=True) runs `gh pr ready --undo` \u2014 converts PR to draft, mutates PR\
        \ state (source :1563,1736)."
    ready:
      beats:
      - interact
      confidence: high
      reason: "cmd_ready runs `gh pr ready` \u2014 marks PR ready for review, mutates PR state (source\
        \ :1563)."
    reopen:
      beats:
      - interact
      confidence: high
      reason: "cmd_reopen runs `gh pr reopen` \u2014 reopens a closed PR, mutates state (source :1497)."
    request-changes:
      beats:
      - interact
      confidence: high
      reason: "cmd_review_action with --request-changes (body required) runs `gh pr review --request-changes`\
        \ \u2014 posts a review, mutates PR (source :1411,1657)."
    review:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_review_summary reads PR detail/checks/threads, classifies risk, prints summary + doctrine\
        \ \u2014 read-only dump, posts nothing. Lexicon principle 1: a read usable for verification stays\
        \ snapshot; the merge gate (not review) is the verify (source :1340)."
    update-branch:
      beats:
      - interact
      confidence: high
      reason: "cmd_update_branch runs `gh pr update-branch` \u2014 merges/rebases base into the PR branch\
        \ on the remote, mutates the branch (source :1554)."
  ghidra:
    analyze:
      beats:
      - interact
      confidence: medium
      reason: "cmd_analyze runs analyzeHeadless -import -overwrite \u2014 imports binary, runs auto-analysis,\
        \ writes a Ghidra project DB to $GHIDRA_PROJECT. A transform/derivation with a persistent project\
        \ side-effect (lexicon principle 3: transforms are interact) (source tools/agent-ghidra:48)."
    decompile:
      beats:
      - interact
      confidence: medium
      reason: "cmd_decompile imports+overwrites a project then runs a decomp postScript and prints C source\
        \ \u2014 a read-shaped derivation that still writes a project DB side-effect. Principle 3 (transforms\u2192\
        interact) puts it in interact, though it borders on snapshot since output is just stdout (source\
        \ :70)."
    strings:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_strings shells out to system `strings -n <min>` \u2014 pure read of the binary, no\
        \ side effects (source :117)."
  git:
    branch:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: high
      reason: 'Bare `branch` lists branches (snapshot); `branch <name>` creates and switches to a branch
        (interact). Beat decided by presence of the name argument at call time (help text + lexicon `branches:
        snapshot`).'
    commit:
      beats:
      - interact
      confidence: high
      reason: "Commits staged changes (auto-generates message if absent) \u2014 mutates repo history.\
        \ Lexicon principle 2 explicitly: for commit-backed flows interact subsumes save; no separate\
        \ save beat invented (help text)."
    stash:
      beats:
      - interact
      confidence: high
      reason: '`stash` and `stash pop` both mutate the working tree and the stash stack; reversible, so
        interact without destructive (per lexicon principle 5 graceful-mutation rule) (help text).'
  hardware:
    printer default:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: high
      reason: 'cmd_default: no arg runs `lpstat -d` to show the default (snapshot); with a printer name
        runs `lpoptions/lpadmin -d` to set it (interact). Beat decided by argument at call time (source
        tools/agent-printer:46).'
    printer print:
      beats:
      - interact
      confidence: high
      reason: "cmd_print runs `lp` to submit a print job \u2014 real-world side effect (source :72)."
    printer queue:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_queue runs `lpq -a`/`lpstat -o` \u2014 read-only dump of the print queue (source :131)."
    serial config:
      beats:
      - snapshot
      confidence: high
      reason: cmd_config runs ONLY `stty -a` (show). Despite the help's 'Show/set port configuration'
        label, the source is read-only; setting raw mode lives in the separate `raw` verb (source tools/agent-serial:111).
        Help/source mismatch noted.
    usb disks:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_disks runs `diskutil list`/`lsblk` \u2014 read-only disk/volume listing (source tools/agent-usb:157)."
    usb eject:
      beats:
      - interact
      confidence: high
      reason: "cmd_eject runs `diskutil eject`/`udisksctl power-off` \u2014 graceful device release. Lexicon\
        \ principle 5: graceful release is plain interact, not destructive (source tools/agent-usb:~76)."
    usb mount:
      beats:
      - interact
      confidence: high
      reason: "cmd_mount runs `diskutil mount`/`udisksctl mount` \u2014 mutates the system mount table\
        \ to make storage usable. Connect-flavored but a system mutation, so interact (source tools/agent-usb:109)."
    usb unmount:
      beats:
      - interact
      confidence: high
      reason: "cmd_unmount runs `diskutil unmount`/`umount` \u2014 graceful release, mutates mount table;\
        \ interact without destructive (source tools/agent-usb:136)."
  harness:
    contracts propose:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - save
      confidence: medium
      reason: contracts_propose derives a lexicon-driven contracts draft from the registry (read-only
        on tracked state). Default prints the proposal/stats to stdout (snapshot, cf. latex `template`
        emits-to-stdout precedent); `--out FILE` exports a markdown inventory (save). Beat flips on the
        --out flag (source tools/agent-harness:156).
    evidence:
      attributes:
      - composite
      beats:
      - save
      confidence: medium
      reason: "evidence build gathers session context + nudge telemetry (internal reads) then writes a\
        \ persisted bundle dir .agent-do-harness/evidence/<run> (jsonl + markdown) for future drill-down\
        \ \u2014 persistence of a result (lexicon principle 2); composite flags the internal gather (source\
        \ tools/agent-harness:609)."
    manifest:
      attributes:
      - polymorphic
      beats:
      - save
      - verify
      confidence: high
      reason: 'Two subcommands: `manifest new` writes a manifest.json of predictions/decisions for a future
        check (save, decide-shaped); `manifest verify` compares before/after eval-case flips against the
        manifest''s predictions and writes a pass/fail verdict (verify). Beat decided by subcommand (source
        tools/agent-harness:762,820).'
    nudges:
      beats:
      - snapshot
      confidence: high
      reason: "nudges effectiveness summarizes nudge telemetry and current compliance limits \u2014 read-only\
        \ report, like stats/history (source tools/agent-harness:~536; help text)."
  homekit:
    scene:
      beats:
      - interact
      confidence: high
      reason: "Registry 'scene: Run scene'; source aliases scene\u2192cmd_run which runs `shortcuts run\
        \ \"$scene\"` to trigger a HomeKit scene \u2014 real-world side effect (source tools/agent-homekit:75,217)."
  ide:
    goto:
      beats:
      - interact
      confidence: medium
      reason: "cmd_goto runs `$ide --goto file:line:col` \u2014 opens the file and moves the cursor; a\
        \ UI navigation mutation of editor state (like focus/scroll = interact) (source tools/agent-ide:90)."
    run:
      attributes:
      - passthrough
      confidence: medium
      reason: "cmd_run dispatches an arbitrary editor command via command-palette keystrokes (osascript)\
        \ \u2014 an opaque editor escape hatch whose effect (read/write/run-task/exec) is unknowable to\
        \ agent-do. Lexicon principle 6, mirroring appleevents `run` passthrough (source tools/agent-ide:117)."
    terminal:
      attributes:
      - passthrough
      confidence: high
      reason: "cmd_terminal types an arbitrary shell command into the IDE integrated terminal (osascript\
        \ keystrokes) \u2014 shell escape hatch, opaque payload (lexicon principle 6, like `shell`) (source\
        \ tools/agent-ide:194)."
  jupyter:
    run:
      beats:
      - interact
      confidence: high
      reason: "cmd_run runs `jupyter nbconvert --to notebook --execute` \u2014 executes all notebook cells,\
        \ producing an executed notebook (a transform/derivation). Runs arbitrary code but on a defined\
        \ artifact (a notebook), not an opaque inline payload, so interact not passthrough (source tools/agent-jupyter:110;\
        \ lexicon `* run`\u2192interact)."
  k8s:
    exec:
      attributes:
      - passthrough
      confidence: high
      reason: 'Source line 126: kubectl exec -it <pod> -- $cmd runs an arbitrary user-supplied command
        inside a pod; arbitrary-code escape hatch.'
    port-forward:
      attributes:
      - long_running
      beats:
      - connect
      confidence: medium
      reason: 'Help+source: kubectl port-forward <pod> <ports> opens a blocking local tunnel session (may
        never return); establishes a usable attachment. interact+long_running also defensible.'
  lab:
    start:
      attributes:
      - long_running
      beats:
      - connect
      confidence: medium
      reason: cmd_start builds and runs 'jupyter lab --port=...' to bring JupyterLab into a usable serving
        state; serve-shaped daemon. interact+long_running also defensible.
  latex:
    compile:
      beats:
      - interact
      confidence: high
      reason: "Help: 'Compile LaTeX to PDF' \u2014 A->B file derivation/transform, no data loss."
  learn:
    correct:
      beats:
      - save
      confidence: high
      reason: cmd_correct (source l.62-114) appends to corrections.json and INSERTs into patterns.db so
        the correction is reused for future routing; learn-shaped persistence.
    patterns:
      beats:
      - snapshot
      confidence: high
      reason: cmd_patterns (source l.173-198) opens patterns.db read-only and prints top 20 learned patterns;
        no side effects.
  logs:
    filter:
      beats:
      - snapshot
      confidence: medium
      reason: Registry 'logs' stub declares filter:'Filter logs' with concurrency:read; no implementation
        in tools/agent-logs. Filtering log content returns a read-only subset = snapshot. Inferred from
        declared read-only semantics, not from running help.
  macos:
    menu:
      beats:
      - interact
      confidence: high
      reason: "Help: 'menu <Menu> <Item>' clicks a macOS menu item \u2014 mutates GUI/app state via accessibility\
        \ action."
  meet:
    video:
      beats:
      - interact
      confidence: high
      reason: "Help: 'video \u2014 Toggle camera' mutates live meeting device state."
  meetings:
    active:
      beats:
      - snapshot
      confidence: high
      reason: detect_active_provider (source) reads process state via pgrep/forced env and reports the
        active provider; read-only.
    end:
      beats:
      - interact
      confidence: high
      reason: "Help: 'end \u2014 End or leave the active meeting' delegates a graceful leave/end; graceful\
        \ release is interact, non-destructive per contract rules."
    meet:
      attributes:
      - polymorphic
      beats:
      - interact
      confidence: medium
      reason: delegate_provider exec's agent-meet with passed args; agent-meet surface (new/join/schedule/mute/video/share/chat/captions/end)
        is entirely interact, so delegated beat resolves to interact. Beat decided by delegated subcommand
        at call time.
    providers:
      beats:
      - snapshot
      confidence: high
      reason: "Help: 'providers \u2014 List supported providers' returns a static read-only list."
    schedule:
      beats:
      - interact
      confidence: high
      reason: 'Help: ''schedule <provider> <topic>'' delegates a provider-specific scheduling flow that
        creates/triggers a meeting; mutating/async trigger.'
    teams:
      attributes:
      - polymorphic
      beats:
      - interact
      confidence: medium
      reason: delegate_provider exec's agent-teams with passed args; agent-teams surface (join/new/call/chat/mute/video/share/status/end)
        is entirely interact, so delegated beat resolves to interact. Beat decided by delegated subcommand.
    video:
      beats:
      - interact
      confidence: high
      reason: "Help: 'video [--provider] \u2014 Toggle camera on the active provider' delegates a device-state\
        \ mutation."
    zoom:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: medium
      reason: delegate_provider exec's agent-zoom; agent-zoom surface includes 'list' (snapshot) plus
        join/start/schedule/end/mute/video/share/record/chat (interact). Beat decided by delegated subcommand
        at call time.
  memory:
    recall:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'recall <key> Recall a memory' \u2014 read-only retrieval of a stored value from\
        \ the persistent memory store, no mutation."
  midi:
    play:
      beats:
      - interact
      confidence: high
      reason: "--help: 'play <file> Play MIDI file' \u2014 emits audio to the MIDI device (world mutation),\
        \ one-shot bounded by file length, returns when done (not a daemon, unlike 'monitor')."
  namecheap:
    dns:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'dns <domain> List DNS records' \u2014 read-only listing of provider DNS records."
    dns-verify:
      beats:
      - verify
      confidence: high
      reason: "--help: 'dns-verify ... Verify exact provider match; add --public-verify to compare public\
        \ DNS' \u2014 purposive pass/fail match check, the canonical dns-check verify shape."
    nameservers:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'nameservers <domain> Show current nameservers' \u2014 read-only display of current\
        \ NS records."
  network:
    ping:
      beats:
      - snapshot
      confidence: medium
      reason: "--help: 'ping <host> Ping a host' \u2014 reports reachability/latency stats; a read usable\
        \ for verification but with no expected-value pass/fail, so snapshot per the status-read rule.\
        \ Straddles verify."
    scan:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'scan [--port] Find what is using a port (or list listening ports)' \u2014 read-only\
        \ inspection of port/process state."
    trace:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'trace <host> Trace route to host' \u2014 read-only traceroute hop dump, no mutation."
    whois:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'whois <domain> WHOIS lookup' \u2014 read-only registration-data dump."
  notion:
    auth:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'auth status Token identity and source' \u2014 read-only identity/source report\
        \ under CONNECT & SNAPSHOT section."
    blocks:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'blocks <page-or-url> Recursive block tree' \u2014 read-only structured dump of\
        \ page block tree."
    bootstrap-team:
      attributes:
      - composite
      beats:
      - snapshot
      - save
      confidence: high
      reason: "engine.py cmd_bootstrap_team: lists remote data sources + adopt_roles (snapshot), then\
        \ save_schema persists adopted role\u2192data-source mapping locally (save); create-missing is\
        \ explicitly blocked, so no remote interact."
    cache:
      beats:
      - snapshot
      confidence: high
      reason: "engine.py cmd_cache handles only 'status' (counts/last_sync) and 'search' (FTS over local\
        \ cache) \u2014 both read-only."
    comment:
      beats:
      - interact
      confidence: high
      reason: "--help: 'comment add <page> --text Add a page/block comment' (WRITE section) \u2014 creates\
        \ a comment in remote Notion, world mutation."
    decision:
      beats:
      - interact
      confidence: high
      reason: "--help: 'decision record --title --content Create a decision record' (WRITE section) \u2014\
        \ creates a remote Notion record; durable but mechanically a remote write."
    handoff:
      beats:
      - interact
      confidence: high
      reason: "--help: 'handoff create --title --content Create an agent handoff record' (WRITE section)\
        \ \u2014 creates a remote Notion record."
    save-group:
      beats:
      - interact
      confidence: high
      reason: "engine.py cmd_save_group: creates a linked hub page plus child pages in remote Notion \u2014\
        \ multiple creations, all the same interact beat (not composite)."
    task:
      beats:
      - interact
      confidence: high
      reason: "--help: 'task add --title [--owner] Create a task record using schema mapping' (WRITE section)\
        \ \u2014 creates a remote Notion row."
    webhooks ingest:
      attributes:
      - composite
      beats:
      - verify
      - save
      confidence: high
      reason: "engine.py cmd_webhooks ingest: optional HMAC signature compare_digest validation (verify)\
        \ then appends the event to events.jsonl (save) \u2014 validate/store in one call."
    workspace:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'workspace Bot, users, data sources, and schema mapping' (CONNECT & SNAPSHOT) \u2014\
        \ read-only workspace state dump."
  ocr:
    file:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'file <path> Extract text from image file' \u2014 reads/perceives image and prints\
        \ extracted text to stdout, no file derived, no mutation."
    region:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'region <x> <y> <w> <h> Extract text from screen region' \u2014 read-only perception\
        \ of screen pixels into text."
    screen:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'screen Extract text from entire screen' \u2014 read-only perception of full screen\
        \ into text."
  okta:
    app-create-oidc native:
      beats:
      - interact
      confidence: high
      reason: "agent-okta type_map native = token_endpoint_auth_method 'none' (PKCE public client); POST\
        \ /apps creates the app and prints '(none \u2014 PKCE app)' \u2014 world mutation, no secret emitted."
    app-create-oidc service:
      attributes:
      - sensitive
      beats:
      - interact
      confidence: high
      reason: "agent-okta type_map service = client_secret_basic; POST /apps creates the app and prints\
        \ 'Client Secret: {secret}' \u2014 world mutation that emits secret material."
    app-create-oidc spa:
      beats:
      - interact
      confidence: high
      reason: agent-okta type_map spa = application_type browser, auth method 'none' (PKCE public client);
        creates app, no client secret emitted.
    app-create-oidc web:
      attributes:
      - sensitive
      beats:
      - interact
      confidence: high
      reason: "agent-okta type_map web = client_secret_basic; POST /apps creates the app and prints the\
        \ client_secret \u2014 world mutation that emits secret material."
    app-create-saml:
      beats:
      - interact
      confidence: high
      reason: "--help: 'app-create-saml <label> Create SAML 2.0 app' (--sso-url/--audience) \u2014 POST\
        \ creates remote SAML app; SAML uses signing cert/metadata, no client secret printed at create."
    app-creds:
      attributes:
      - sensitive
      beats:
      - snapshot
      confidence: high
      reason: "agent-okta app-creds: GETs client_id and client_secret and prints the secret (full with\
        \ --reveal, else masked) \u2014 read-only retrieval that emits secret material."
    auth-servers:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'auth-servers List authorization servers' \u2014 read-only listing."
    trusted-origins:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'trusted-origins List trusted origins' (ADMIN section, distinct from trusted-origin-add/del\
        \ writes) \u2014 read-only listing."
  pdf:
    extract:
      beats:
      - snapshot
      confidence: high
      reason: 'Source: case read|extract|text -> cmd_read; outputs PDF text to stdout via pdftotext, no
        side effects.'
  pdf2md:
    batch:
      beats:
      - interact
      confidence: high
      reason: 'Source cmd_batch: mkdir -p outdir, loops PDFs, calls cmd_convert writing .md files (A->B
        file derivation transform). Registry mislabels concurrency:read but it writes derived files.'
  printer:
    print:
      beats:
      - interact
      confidence: high
      reason: "--help: sends a file to the printer (lp/lpr), creating a queued print job and physical\
        \ output \u2014 world-mutating async trigger, not destructive."
  prompt:
    load:
      beats:
      - snapshot
      confidence: high
      reason: 'Source cmd_load: cat of saved prompt .txt to stdout; pure read of stored agent-side state,
        no mutation.'
    run:
      beats:
      - snapshot
      confidence: medium
      reason: "Source cmd_run: reads stored prompt, substitutes {{vars}}, echoes to stdout. No file write\
        \ and no execution despite the name \u2014 borderline pure templating transform; classed snapshot\
        \ since it has zero side effects (could be argued interact as A->B render)."
  psql:
    sample:
      beats:
      - snapshot
      confidence: high
      reason: '--help: sample n rows (default 5) from a table; read-only SELECT returning data.'
  render:
    notify override:
      beats:
      - snapshot
      confidence: high
      reason: "Source cmd_notify override branch: GET /notification-settings/overrides/services/$sid \u2014\
        \ read-only fetch of one service's notification override, not a write."
    notify overrides:
      beats:
      - snapshot
      confidence: high
      reason: "Source cmd_notify overrides branch: GET /notification-settings/overrides?ownerId \u2014\
        \ read-only list of owner overrides."
    rollback:
      beats:
      - interact
      confidence: high
      reason: "Source cmd_rollback: POST /services/$sid/rollback with deployId \u2014 deploy-style async\
        \ trigger that redeploys a prior version; mutating, reversible (no data loss) so not destructive."
  repl:
    spawn:
      beats:
      - connect
      confidence: high
      reason: '--help: spawn <type> starts a REPL session (python/node/psql/etc.) returning a session
        id used by send/read; brings tool into usable session state. Verb returns immediately so not long_running.'
  screen:
    cursor:
      beats:
      - snapshot
      confidence: high
      reason: "--help: 'cursor \u2014 Get current cursor position'; read-only query of pointer position,\
        \ no side effects."
  sessions:
    grep:
      beats:
      - snapshot
      confidence: high
      reason: Read-only regex search within a stored session transcript; help + bin/agent-sessions show
        FTS/regex query with no mutation.
    turns:
      beats:
      - snapshot
      confidence: high
      reason: Read-only dump of last N transcript turns (--last/--role/--limit); no side effects per --help.
  slack:
    dm:
      attributes:
      - composite
      beats:
      - interact
      confidence: high
      reason: "Source resolves user, then conversations.open, then posts message \u2014 one call performs\
        \ resolve+open+send, deliverable is a sent message (mutate world)."
    resolve-user:
      beats:
      - snapshot
      confidence: high
      reason: Read-only lookup resolving a name/email/ID to a Slack user ID; no mutation in help or source.
  sms:
    code:
      beats:
      - verify
      confidence: high
      reason: "handle_code unconditionally calls wait_for_message (blocks until a matching SMS or times\
        \ out), then extracts code \u2014 block-until-condition with pass/fail timeout."
    latest:
      beats:
      - snapshot
      confidence: high
      reason: handle_latest calls filtered_messages with no wait loop; pure read of the latest matching
        message.
    link:
      beats:
      - verify
      confidence: high
      reason: "handle_link unconditionally calls wait_for_message (blocks until matching SMS or times\
        \ out), then extracts magic link \u2014 block-until-condition."
  ssh:
    download:
      beats:
      - interact
      confidence: high
      reason: cmd_download runs scp host:src -> local dst, writing a local file from a remote source (A->B
        transfer/derivation); agent-side state mutation, not data loss.
    exec:
      attributes:
      - passthrough
      confidence: high
      reason: "cmd_exec runs `ssh host \"$cmd\"` with the user's arbitrary command \u2014 arbitrary-code\
        \ escape hatch on the remote host; effect entirely determined by the command."
  supabase:
    addons:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_addons GET /projects/<ref>/billing/addons \u2014 read-only list of billing addons."
    db-connect:
      attributes:
      - composite
      - sensitive
      beats:
      - connect
      - save
      confidence: high
      reason: Fetches DB host via Management API, persists an agent-db profile including SUPABASE_DB_PASSWORD
        (run_agent_db profile add --password), then connects; idempotent reuse if profile exists. Persists
        secret + connect = composite, sensitive.
    domain-init:
      beats:
      - interact
      confidence: high
      reason: "cmd_domain_init POSTs /custom-hostname/initialize \u2014 creates/initializes a custom hostname\
        \ config (state mutation)."
    domain-reverify:
      beats:
      - verify
      confidence: high
      reason: "cmd_domain_reverify POSTs /custom-hostname/reverify to re-check custom hostname DNS and\
        \ return verification state \u2014 purposive dns-check (verify beat per definition)."
    health:
      beats:
      - snapshot
      confidence: high
      reason: cmd_health GET /projects/<ref>/health prints service status; status reads are snapshot,
        not verify, per contract.
    network-bans:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_network_bans POSTs /network-bans/retrieve \u2014 despite POST verb it only retrieves/lists\
        \ banned IPs (read-only retrieval)."
    network-restrict:
      beats:
      - interact
      confidence: high
      reason: "cmd_network_restrict POSTs /network-restrictions/apply with CIDRs \u2014 applies (mutates)\
        \ network restrictions."
    network-restrictions:
      beats:
      - snapshot
      confidence: high
      reason: "GET /projects/<ref>/network-restrictions \u2014 read-only display of allowed CIDRs."
    network-unban:
      beats:
      - interact
      confidence: high
      reason: "cmd_network_unban DELETEs /network-bans with IP body \u2014 removes a ban (config mutation\
        \ that restores access; not irreversible data loss, so not destructive)."
    pause:
      beats:
      - interact
      confidence: high
      reason: "cmd_pause POSTs /projects/<ref>/pause \u2014 pauses the project (state mutation, reversible\
        \ via restore; not data loss)."
    postgrest:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_postgrest GET /projects/<ref>/postgrest prints PostgREST config \u2014 read-only."
    replica-setup:
      beats:
      - interact
      confidence: high
      reason: "cmd_replica_setup POSTs /read-replicas/setup (requires --yes, billable) \u2014 async provisioning\
        \ trigger (deploy-style mutation)."
    sample:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_sample calls ensure_db_connected (lazy idempotent attach to an existing profile, errors\
        \ if none) then run_agent_db sample \u2014 deliverable is read-only sample rows; connect is a\
        \ precondition not a deliverable."
    types:
      beats:
      - snapshot
      confidence: high
      reason: cmd_types GET /types/typescript returns generated TS types to stdout; read-only derivation
        with no persistence or state mutation (not a file-writing transform).
    upgrade:
      beats:
      - interact
      confidence: high
      reason: "cmd_upgrade POSTs /projects/<ref>/upgrade (requires --yes, causes downtime) \u2014 async\
        \ state-mutating trigger; irreversible version change but not data-loss, so not flagged destructive."
    vanity:
      beats:
      - snapshot
      confidence: high
      reason: "cmd_vanity GET /vanity-subdomain \u2014 read-only display of vanity subdomain config."
  swarm:
    parallel:
      attributes:
      - composite
      beats:
      - interact
      confidence: medium
      reason: "cmd_parallel runs multiple `agent-do --dry-run` tasks as background processes and waits\
        \ for all \u2014 multi-run orchestration (composite); dry-run mutes real mutation."
    pipeline:
      attributes:
      - composite
      beats:
      - interact
      confidence: medium
      reason: "cmd_pipeline runs `agent-do --dry-run` tasks sequentially, stop-on-failure \u2014 sequenced\
        \ multi-run orchestration (composite); dry-run mutes real mutation."
    spawn:
      attributes:
      - composite
      beats:
      - interact
      confidence: medium
      reason: "cmd_spawn launches N parallel sub-runs of `agent-do --dry-run <task>`, waits, and persists\
        \ a swarm results JSON for later retrieval \u2014 orchestration/agent-side mutation; --dry-run\
        \ mutes real world side effects, lowering confidence on the beat."
  tail:
    errors:
      beats:
      - snapshot
      confidence: high
      reason: "Reads captured log files and filters error/warning lines \u2014 read-only view of logs\
        \ (status/logs read = snapshot, not verify)."
    grep:
      beats:
      - snapshot
      confidence: high
      reason: "Searches captured log files for a pattern \u2014 read-only log search."
    run:
      attributes:
      - passthrough
      - long_running
      confidence: high
      reason: "Foreground wrapper that executes arbitrary 'name: cmd' dev commands, streaming to terminal+logs\
        \ until Ctrl+C \u2014 arbitrary-code escape hatch and a long-running foreground process."
    start:
      attributes:
      - passthrough
      - long_running
      confidence: high
      reason: "Background launch of arbitrary 'name: cmd' dev commands as logged services, returning immediately\
        \ \u2014 arbitrary-code escape hatch spawning a long-running background service."
  unbrowse:
    replay:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: medium
      reason: Sources captured api.sh and calls the named curl-wrapper function; HTTP method/effect depends
        on which captured endpoint is invoked at call time (GET=read vs POST/DELETE=write), so the beat
        is payload-decided.
  usb:
    eject:
      beats:
      - interact
      confidence: high
      reason: "Source: diskutil eject / umount \u2014 graceful release of a mounted device; flushes and\
        \ detaches without data loss. Rule: graceful release = interact, not destructive."
    mount:
      beats:
      - connect
      confidence: medium
      reason: "Source: diskutil mount / mount <device> \u2014 attaches USB storage to a usable state.\
        \ Reads as the connect (idempotent attach) counterpart to eject; arguably interact since it mutates\
        \ the OS mount table and there is no tool-session concept."
  vector:
    ask:
      beats:
      - interact
      confidence: high
      reason: "cmd_ask wraps cmd_snapshot \u2192 POST project_updates, creating an ask-typed update record.\
        \ Despite the name it is a write, not a read."
    bind:
      beats:
      - interact
      confidence: high
      reason: "cmd_bind: POST project_bindings with on_conflict=project_id (upsert) registering project\u2194\
        dir/repo/render-svc/supabase-ref. Mutates backend config; a write, not a read."
    feed:
      beats:
      - snapshot
      confidence: high
      reason: 'cmd_feed: GET activity feed with --since/--limit. Read-only.'
    intake:
      attributes:
      - polymorphic
      - composite
      beats:
      - snapshot
      - interact
      confidence: high
      reason: "cmd_intake: list=GET (snapshot); new=POST, decline=PATCH (interact); accept=POST projects\
        \ THEN PATCH intake (two writes in one call \u2192 composite). Beat depends on sub-action."
    link:
      attributes:
      - destructive
      beats:
      - interact
      confidence: high
      reason: "cmd_link: add=POST project_links, rm=DELETE project_links?id=eq \u2014 the rm path is an\
        \ irreversible hard delete of a link record (destructive), add is a non-destructive write."
    members:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: high
      reason: 'cmd_members: list=GET project_members (snapshot), add=POST upsert (interact). Beat selected
        by sub-action arg.'
    support:
      beats:
      - interact
      confidence: high
      reason: 'cmd_support: add=POST project_support_items, resolve=PATCH status=resolved. Both sub-actions
        are writes.'
    today:
      beats:
      - snapshot
      confidence: high
      reason: 'cmd_today: GET-only read of owned projects/open work/commits. No side effects.'
    work:
      beats:
      - interact
      confidence: high
      reason: 'cmd_work: add=POST project_work_items, done/block=PATCH status. All sub-actions mutate
        backend records.'
  vercel:
    teams:
      beats:
      - snapshot
      confidence: high
      reason: "Help: 'List teams' \u2014 read-only enumeration of account teams."
  video:
    extract:
      beats:
      - interact
      confidence: high
      reason: "Help: 'Extract frames as images' into a dir \u2014 A\u2192B file derivation (transform),\
        \ classified interact per contract."
  vision:
    detect:
      attributes:
      - polymorphic
      beats:
      - snapshot
      - interact
      confidence: medium
      reason: "Default 'Run YOLO on current frame' emits detections (read-only perception \u2192 snapshot);\
        \ --annotate writes a derived annotated image (transform \u2192 interact). Beat flips by flag."
    faces:
      beats:
      - snapshot
      confidence: high
      reason: "'Detect faces' / --identify \u2014 read-only perception of the current frame; no save (face\
        \ learning is a separate 'face learn' command)."
    ocr:
      beats:
      - snapshot
      confidence: high
      reason: "'Extract all text' / --region / --find \u2014 read-only perception of the current frame,\
        \ no world mutation."
    source file:
      beats:
      - connect
      confidence: high
      reason: 'Under SOURCE MANAGEMENT: attaches a video file as the active input source (paired with
        ''disconnect''/release). Establishes the usable-state attachment before snapshot.'
    source image:
      beats:
      - connect
      confidence: high
      reason: "SOURCE MANAGEMENT: sets a single image as the active source \u2014 attach/usable-state\
        \ establishment, no perception output itself."
    source ios:
      beats:
      - connect
      confidence: high
      reason: 'SOURCE MANAGEMENT: attaches the iOS simulator (via agent-ios) as the live input source.
        Connect/attachment beat.'
    source rtsp:
      beats:
      - connect
      confidence: high
      reason: 'SOURCE MANAGEMENT: attaches an RTSP/IP camera stream URL as the active source. Establishes
        attachment; streaming/blocking happens later in watch/record.'
    source screen:
      beats:
      - connect
      confidence: high
      reason: 'SOURCE MANAGEMENT: attaches a display as the capture source. Connect/attachment beat.'
    source webcam:
      beats:
      - connect
      confidence: high
      reason: 'SOURCE MANAGEMENT: attaches a webcam (default index 0) as the active input device. Connect/attachment
        beat.'
    source window:
      beats:
      - connect
      confidence: high
      reason: 'SOURCE MANAGEMENT: attaches a named macOS window as the capture source. Connect/attachment
        beat.'
  vm:
    start:
      beats:
      - interact
      confidence: high
      reason: "Help: 'Start VM' \u2014 powers on a virtual machine via hypervisor CLI. World-state mutation\
        \ (deploy-style async trigger), not a tool-session attach. Non-destructive."
  voice:
    listen:
      beats:
      - snapshot
      confidence: medium
      reason: "cmd_listen: records a bounded duration (default 5s) to a temp file, transcribes to text,\
        \ then deletes the temp file. Net result is a perception (audio\u2192text) with no persisted world\
        \ mutation \u2014 analogous to a snapshot capture. Bounded, so not long_running."
    speak:
      beats:
      - interact
      confidence: high
      reason: "cmd_speak: invokes say/espeak to play synthesized audio \u2014 an output action with a\
        \ real-world side effect (sound). Mutates the world, not a read."
  wireshark:
    filter:
      beats:
      - interact
      confidence: high
      reason: "cmd_filter: tshark -r <file> -Y <filter> -w <output> (default filtered.pcap) \u2014 reads\
        \ a pcap and writes a new derived pcap. A\u2192B file derivation (transform) \u2192 interact,\
        \ not a read-only display."
  zoom:
    start:
      beats:
      - interact
      confidence: high
      reason: "cmd_start runs `open zoommtg://zoom.us/start` to launch Zoom into a new instant meeting\
        \ \u2014 world-side trigger, not idempotent attach (tools/agent-zoom:99-107)"
    video:
      beats:
      - interact
      confidence: high
      reason: cmd_video sends cmd+shift+V keystroke to toggle camera state; check_zoom is a precondition
        guard, not connect (tools/agent-zoom:186-198)
  zpc:
    decide-batch:
      beats:
      - save
      confidence: high
      reason: "Reads piped/file pipe-delimited decisions and appends them to decisions.jsonl for future\
        \ sessions \u2014 learn-shaped persistence, append-only (lib/memory.sh:154-263)"
    patterns:
      beats:
      - snapshot
      confidence: high
      reason: "Reads patterns.md and prints it; --score additionally reads lessons.jsonl + git log to\
        \ compute/print effectiveness, but writes nothing \u2014 read-only in both modes, flag does not\
        \ flip beat (lib/intelligence.sh:317-452)"
